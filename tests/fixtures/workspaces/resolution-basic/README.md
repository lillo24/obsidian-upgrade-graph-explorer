# Basic workspace resolution fixture

This synthetic two-note workspace proves unique filename resolution, repeated
occurrence preservation, document preamble ownership, nested section ownership,
and links contained in heading syntax. KG4 parses these files only in tests;
the production resolver receives already-parsed adapter IR.
