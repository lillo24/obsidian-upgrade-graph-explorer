# Obsidian block marker fixture

This synthetic source covers inline and standalone explicit block IDs,
duplicates, invalid characters, and marker-looking text shielded by code and
comments. The adapter reports exact marker spans without guessing a full
Markdown block extent.
