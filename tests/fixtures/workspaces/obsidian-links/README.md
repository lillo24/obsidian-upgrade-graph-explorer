# Obsidian link fixture

This two-file synthetic workspace keeps representative wikilink, heading,
same-file, embed, and Markdown-link syntax together for adapter regression
coverage. KG3 parses the targets but deliberately does not resolve them to
`B.md` or any canonical entity.
