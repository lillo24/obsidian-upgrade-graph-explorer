# Section extents

This synthetic CommonMark file exercises document preamble, skipped heading
levels, duplicate titles, nested sections, sibling closure, and root-heading
closure in one inspectable source tree.

The parser test keeps the expected assertions close to the behavior. No golden
snapshot is stored because this fixture represents parser intermediate
structure, not a canonical workspace snapshot.
