# Ambiguous file resolution fixture

Two notes share a basename. The fixture preserves ambiguity for the bare name,
tests folder-qualified matching and heading-based narrowing, and proves that a
YAML alias is metadata rather than a persisted link destination.
