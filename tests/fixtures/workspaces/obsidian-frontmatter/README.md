# Obsidian frontmatter fixture

This synthetic case proves that leading YAML is source metadata rather than a
CommonMark Setext heading. The adapter extracts a list of document aliases and
retains only the real Markdown heading in KG2-compatible structure.
