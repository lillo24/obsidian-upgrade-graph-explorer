# Unsupported attachment fixture

The parsed embed remains a canonical reference, but a Markdown-only parsed
inventory cannot establish whether an image exists. KG4 reports the target type
as unsupported rather than mislabeling the attachment as a missing note.
