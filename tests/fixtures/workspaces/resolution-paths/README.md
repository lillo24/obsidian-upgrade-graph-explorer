# Path resolution fixture

This case covers source-relative wikilinks and Markdown paths, traversal above
the workspace root, URL-encoded spaces in filenames/headings, and malformed
percent encoding. Canonical references keep adapter `rawTarget` unchanged.
